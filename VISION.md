# Vision

`PRODUCT.md` is the product vision single source of truth for Transcriptor.

Read `PRODUCT.md` before product, UX, release, or architecture changes. This
file exists as the stable entrypoint expected by audit and implementation
workflows, while the canonical product contract remains in `PRODUCT.md`.

# BUGSAUDIT-2026-09-04 — костыли, workaround'ы, хардкод, SSOT (Ultra-Audit)

## 1. Шапка
Дата: 2026-09-04. Ревизия: `3b8f5a8b9fc1c6c50cdddac377c4d53772d609fb` (main). Проверка на этой ревизии: бэкенд `unittest discover -s backend/tests` → 652 OK; фронтенд `vitest` → 281 passed; desktop `node --test` → ℹ tests 208 ℹ pass 208 ℹ fail 0 . Метод: три обязательных обхода (история `git log -p -40`, структура с таблицей покрытия, сценарии и швы) тремя аудиторами по доменам на замороженных снимках; 12 находок бэкенда воспроизведены исполнением на бандловом интерпретаторе. Известное из `BUGS_AUDIT_2026-09-03.md` не повторяется.

## 2. Покрытие
Таблицы покрытия «область → просмотрено/частично/не смотрел → чем → почему» — в разделах по доменам:
- `docs/audit-2026-09-04/backend-findings.md` (90 находок: P0 2 · P1 23 · P2 65; подтверждено 72, гипотез 18; индекс костылей 39, хардкод→SSOT 20; §7 — чистые места)
- `docs/audit-2026-09-04/frontend-findings.md` (96 находок, 91 уникальная: P0 1 · P1 23 · P2 67; подтверждено 80, гипотез 11; workaround'ов 49, хардкод→SSOT 68)
- `docs/audit-2026-09-04/desktop-findings.md` (84 находки: P0 3 · P1 26 · P2 55; подтверждено 77, гипотез 7; костылей 12, хардкод→SSOT 26; 20 проверенных корректных мест)
Не аудировано: `backend/deepgram_recovery.py` (появился после снимка; аудит поручён фикс-агенту бэкенда), `frontend/src/main.tsx` после `889c91a` (аудит шёл по снимку `0de0c2d`; удалённый стоп-путь — часть находок снята удалением).

## 3. Находки
Полными блоками (ID · файл:строка и символ · суть · воспроизведение · последствие · severity · текущий/исправленный код · почему баг) — в трёх файлах выше. Итого 270 занумерованных (265 уникальных): P0 6, P1 72, P2 187.

## 4. Журнал решений
В журналах по доменам (`docs/audit-2026-09-04/*-fix-journal.md`, раздел «decisions» после каждого коммита). Ключевые решения сессии — `docs/NEXT_SESSION_2026-09-04b.md` §2.

## 5. Журнал правок
Коммит → ID → файлы → проверка: `docs/audit-2026-09-04/*-fix-journal.md`. Сводно (все P0 закрыты):
- бэкенд: `d590a4e` (B-001, B-002), `3b8f5a8` (B-005, B-007) — 631 → 652 тестов;
- рендерер: `74ff589` (U-001 P0), `0c916b9`, `3ee4cb9` (F-001), `585b45f` (F-002), `9afe53e` (C-001…C-003 и др.), `06f3330` (C-006), `58bafe8`, `27fba8d`, `0bf8ed7`, `f09c904`, `b0eecd1` — 196 → 281 тестов;
- desktop: `a152ec7` (D-001 P0), `317b4ca` (D-002 P0), `1d9fda2` (D-003 P0), `22c6e3d` (D-004), `86d63d3` (D-005 и др.), `966bcf7`, `19b0235`.

## 6. Не сделано
Фикс-волны остановлены по лимиту токенов сессии; незакоммиченные правки трёх агентов — ветка `wip/ultra-audit-2026-09-04` (не проверена). Поимённо по ID — в разделах «не сделано» журналов; порядок доводки — `docs/NEXT_SESSION_2026-09-04b.md` §4–§5. Оценка по журналам: P1 закрыты ≈ 60–70 % по доменам (точные ID — в журналах), P2 — начаты (SSOT-индексы и workaround-индексы пройдены частично).

## 7. Подготовлено, но не выполнено
- Релиз 1.6.1 (CHANGELOG, версия, сборка, установка) — команды в `docs/NEXT_SESSION_2026-09-04b.md` §5.
- Нотаризация Developer ID (`1d9fda2` делает сборку подписываемой; `notarytool` здесь не запускался).
- Проверка Windows/Linux путей вставки на реальном железе.

# Продолжение работ после сессии 2026-09-03/04

Точка входа для следующей сессии. Контекст: `BUGS_AUDIT_2026-09-03.md` (аудит, дополнение, реестр долга, «кто на чём остановился»). Здесь — точные ТЗ, чтобы агенты могли продолжить без повторного разбора. Правила: AGENTS.md (root cause, без костылей, SSOT, сьюты до коммита, push после); оркестрация субагентами (Sonnet — лёгкие, Opus — сложные); всё новое — в реестр долга, не в расширение задачи.

Сохранённые материалы (вне git): `~/Library/Application Support/transcriptor/evidence-2026-09-03/` — wav записей 3–4 сентября, транскрипты (`session-2026-09-03-04/agent-transcripts/*.output` — полные JSONL всех агентов, включая остановленных), скрипты A/B и разбор лога (`session-2026-09-03-04/scratchpad/`, там же `all.log` — склейка трёх main.log за 10 дней).

## A. Хотфиксы в `main` (агенты были остановлены до первой правки — дерево чистое)

### A1 — бэкенд (Opus): после `Finalize` ждать финалы, пока хвост не покрыт
Лог 2026-09-03T21:42:09Z, сессия `62115e77`: `connected in 6259 ms`; на стопе `finalize ENTER segments_final=0 bytes_sent=456192` (14.26 с); через 130 мс `is_final 0.00–10.85 speech_final=True` и сразу `post-Finalize transcript received (… covered=0.00s gap=14.26s speech_in_gap=6.00s)` → `finalize EXIT 405 ms` → конверт. Второй финал `10.85–14.26 'Напиши, на чем кто вас поверил.'` пришёл через 2.7 с и потерян.
Сделать в `backend/remote_deepgram_live.py::drain_transcript()`:
1. Вместо одного ожидания `_final_arrived` — цикл: после каждого финала пересчитать `_tail_coverage()`; пока `_tail_needs_flush(gap, speech)` (или последний финал `speech_final=False` с gap > `COVERAGE_GAP_MIN_SEC`) — сбросить событие и ждать следующий финал в пределах ОДНОГО дедлайна, вычисленного на входе (объявленный `budgetMs` остаётся верхней границей, не продлевать). `Finalize` повторно не слать. Исчерпан бюджет — существующий tail-guard как сейчас.
2. Лог: `post-Finalize finals=N covered=… gap=… waited=…ms`.
3. Тесты (`test_deepgram_finalize_budget.py`): два финала 0–10.85 и 10.85–14.26 через 2.7 с при streamed 14.26 → оба в конверте, ожидание ≈2.7 с в бюджете; один покрывающий финал → сразу; исчерпание бюджета → tail-guard один раз; объявленный бюджет ≥ измеренного ожидания.
4. Диагностика для A2/дефекта «трёх»: в блок `coverage holes` добавить строку на каждое interim-слово, признанное покрытым ДРУГИМ словом финала (разные буквенные ядра) с обоими словами и временами. Правило 25 % пока не менять.
Проверка: `/Applications/Transcriptor.app/Contents/Resources/runtime/python/bin/python3 -m unittest discover -s backend/tests` (база 475).

### A2 — рендерер (Opus): неполный конверт не завершает гонку; восстанавливать только настоящий хвост
D1 (та же сессия `62115e77`): `decision=RECOVER reason=tail-gap`, `strategy=PARALLEL`, затем `race-first envelope ms=130 … decision=USE_ENVELOPE (+1 words)` — гонка закончилась конвертом, покрывающим 0–10.85 из 14.26 с; полная расшифровка не ждалась. Сделать один чистый предикат в `frontend/src/live-coverage.ts` рядом с `decideDeadStreamRecovery` (переиспользовать `TAIL_GAP_THRESHOLD_SEC`): `envelopeCoversRecording({streamedSec, coveredEndSec, uncoveredSpeechSec, recordedSec})` → false при `streamedSec − coveredEndSec > порога` или `uncoveredSpeechSec > 0`. В гонке: если первым пришёл неполный конверт — ждать recovery-кандидата в рамках бюджета гонки, лог `race-first envelope incomplete covered=10.85/14.26 → waiting for recovery`. В confirm-ветке тот же предикат включает recovery. Тесты на предикат.
D2 (сессия `a9fd3fd9`, 21:43:06Z): финалы «…в одном из» | «последних сообщений. Посмотри внимательно.», слово «трёх» было только в interim; `recoveredTailText` (волна 3) вернул «трёх в» и приклеил В КОНЕЦ. Правило: рендерер восстанавливает только слова, чьё время (по `interimSegment.start/end` и `end` финалов) позже конца вытеснившего финала; слова внутри покрытого времени — только бэкенд-сшивка; без таймстампов — не восстанавливать. Регрессия: финалы [4.07–7.66] и [7.66–10.30] + interim [4.07–6.10] «…из трёх в» → ровно два финала; позитив: interim, выходящий за конец последнего финала на ≥0.3 с → слова добавлены в конец.
Проверка: `npm --prefix frontend run typecheck | lint | test | build` (база 180).

### A3 — desktop (Sonnet): не платить за AX-верификацию там, где она не работает
Лог 21:42:10Z/21:43:07Z: `robust_paste` 1543 мс (было 216), всегда `OK:menu-paste-primary:unverified` для Claude — две AX-попытки по 0.5 с сгорают впустую. Сделать `desktop/paste-verification-policy.js` (чистый, с тестами): ключ — bundle id / имя приложения цели; `shouldAttemptVerification(key)`, `recordOutcome(key, verified|unverified|error)`; после 2 «unverified» подряд верификация для этого приложения отключается до конца процесса (лог один раз), «verified» сбрасывает. `robustPasteScript(verify: boolean)`: при false — без AX-чтений, plain `OK:menu-paste-primary` (парсер уже трактует как unverified — проверить тестом), прежний таймаут osascript. При верификации — чтения по 0.25 с, время чтений в paste-trace. Правило восстановления буфера не менять. `build.files` в `desktop/package.json` дополнить. Проверка: `npm --prefix desktop test`, `node --check desktop/main.js desktop/preload.js` (база 91).

Затем: релизёр (CHANGELOG в стиле файла, все сьюты, коммит, push), `./BUILD.command`, `osascript -e 'tell application "Transcriptor" to quit'`, `open -a /Applications/Transcriptor.app`, проверка boot-лога.

## B. Worktree-ветки (WIP-снимки, запушены на origin; НЕ проверены; перебазировать на `main` и доделать по одной)

Каталоги `.claude/worktrees/agent-*`; смотреть `git diff bd3faef..<ветка>`.

### B1 — `worktree-agent-ad149f499ad8694f3` — тёплый сокет Deepgram (Opus, бэкенд)
Есть: `backend/deepgram_warm.py` (пул), правки `main.py`/`remote_deepgram_live.py` (~294 строки). Остановлен на «probe watchdog task and task wiring». ТЗ: `DeepgramWarmPool` — не более одной тёплой сессии на ключ `DeepgramLiveConfig.to_query_string()`; `acquire(cfg)`/`rewarm(cfg)`/`close_all()`; решение KeepAlive vs 100 мс кадры тишины — по документации Deepgram (https://developers.deepgram.com/docs/keep-alive, /docs/audio-keep-alive), цитату в докстринг; ограничить время жизни (например 5 мин idle — Deepgram тарифицирует подключённое время); при усыновлении вычесть `warm_audio_offset_sec` в ОДНОМ месте нормализации сообщений (сегменты, interim-слова, покрытие, `coveredEndSec`, `streamedSec`) с явным тестом; liveness: мёртвый тёплый сокет отвергать до усыновления, после — переподключение с реплеем буфера, если 2.5 с нет результатов; перевзвод после `shutdown` и при старте бэкенда; `GET /api/live/warm`; лог `adopted warm socket age=… (saved ~…ms connect)`; тесты `test_deepgram_warm.py`.

### B2 — `worktree-agent-a90710620c8f16c09` — pre-roll и тёплый микрофон (Opus, рендерер)
Есть: `frontend/src/capture-warm.ts`, правки `main.tsx`, `pcm-worklet.js` (~579 строк). Остановлен на «track listeners and the AudioContext/worklet branches». ТЗ: warm hold после записи (`UI_TOKENS.capture.warmHoldMs`, 30 000, 0 — выкл.; снять при hide/sleep/смене устройства/TTL; повторный `startLive` без getUserMedia/AudioContext); кольцевой буфер 500 мс в worklet в состоянии «armed», на `start` сначала выдаётся pre-roll (помечен), рендерер добавляет его в PCM-sink, в WS и в отсчёт длительности/порога короткой записи (одно место); pre-roll старше 2 с отбрасывать; на Bluetooth (AirPods/«Bluetooth»/«Hands-Free») warm hold не держать (HFP); `maxPendingSamples` 2048 → меньше с обоснованием; трассы `warm=1/0 preRollMs=N`; чистая логика в модуле с vitest. Проверка — четыре команды фронтенда (база 180).

### B3 — `worktree-agent-a5848078355fe4f2b` — надёжность вставки (Opus, desktop)
Есть: `desktop/paste-capability.js` (+тест), правки `main.js`, `paste-result.js` (~280 строк). Остановлен на «tests for the receipt behaviour». ТЗ: машина состояний Unknown/Untrusted/Active/Broken (`systemPreferences.isTrustedAccessibilityClient(false)` + реальная проба через System Events с таймаутом 1 с; trusted + проба упала = Broken; проба при загрузке, при фокусе, перед первой вставкой; WARN + статус в рендерер с текстом починки «Privacy & Security → Accessibility: убрать и добавить Transcriptor»); контракт вставки — ≤5 попыток, таблица бюджетов одним SSOT с тестом «суммарный worst case ≤ дедлайна post-stop», при неудаче текст остаётся в буфере, старый буфер не восстанавливается, статус называет хоткей paste-last, `last_transcript.json` записан; пометка буфера `org.nspasteboard.TransientType` если Electron позволяет без нативного кода; гонка отпускания модификаторов для Alt+Shift+V (задержка ≥150 мс + явные key-up); Windows — проба `null` = Unknown → как Active.

## C. Реестр долга — см. `BUGS_AUDIT_2026-09-03.md`, раздел «Реестр долга (после волны 3)».
